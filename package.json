{
  "name": "ts_gyakorlas",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc --watch -p ./",
    "live-server": "live-server ./public/",
    "dev": "nodemon scripts/app.tsx",
    "start": "concurrently \"npm run tsc\" \"npm run live-server\""
  },
  "author": "Bela",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "live-server": "^1.2.2",
    "nodemon": "^3.1.9"
  }
}
